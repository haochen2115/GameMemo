# 实验日志

每一轮实验记录假设、设置、结果和结论。dev 上的结果只用来做决定；进入 main 的依据是封存 test 上预登记的那一次运行（见 `docs/BRANCHING.md`）。

环境：4 核 CPU，本地 Ollama。写入模型 `qwen2.5:3b`（Q4_K_M），`temperature=0.1`，`seed=0`；向量模型 `jina-v2-base-zh`（fastembed）。

---

## R0 检索：拒答回退（2026-09-24，`research/personal-memory-foundation`）

- **问题**：v1-hybrid（bge-small-zh）在 retrieval_v1 test 上拒答率 0.5，门禁不过。
- **做法**：建立 retrieval_v2（test 为两位封存的新玩家）；只在 dev 上比较向量模型和 reranker。
- **结果（dev）**：jina-zh 0.929 > bge-m3 0.914 > bge-small 0.871 = bge-reranker 0.871 ≫ jina-reranker 0.393。
- **结论**：预登记 v2-hybrid，test 0.892 vs v0 0.750，合入 main。详见 BENCHMARK.md。

---

## E1 写入链路：小模型上的端到端表现（`research/personal-memory-write-path`）

**起因**：用 qwen2.5:3b 做冒烟测试时发现：提示词示例被写进了记忆、助手的建议被当成玩家事实、段位更新被漏掉、一次超时就让对话崩溃。前两类 bug 已在 main 上修复；其余的用 `e2e_v1` 量化。

**基线（dev p_support，16 题）**

| 系统 | E2EScore | 说明 |
|---|---|---|
| v0-pipeline | 0.188 | 小模型下 JSON 解析全部失败，**一条记忆都没写入**；分数只来自负例（什么都不返回） |
| v2（main） | 0.500 | 9 条记忆；漏抽严重，没有时间信息 |

同时修了一个判分器 bug：答案关键词写成"6月28"时，"2026-06-28"会被判错。现在判分器会把 ISO 日期也展开成"M月D日"。

**R1（dev p_support，16 题，已用修正后的判分器重算）**

| 变体 | E2EScore |
|---|---|
| v2 | 0.500 |
| v2 + 只读玩家发言（W1） | **0.750** |
| v2-slots（W2） | 0.375 |
| v2-slots + 历史召回（W4） | 0.500 |
| v2-slots + W1 + W4 | 0.562 |

**R2（加入第二个 dev 玩家 p_mage2，共 32 题）**

| 变体 | E2EScore | Answer@3 | 过时率 | 时间类 |
|---|---|---|---|---|
| v2 | 0.531 | 0.423 | 0.250 | 0.200 |
| v2 + W1 | 0.562 | 0.462 | 0.125 | 0.400 |
| v2 + W1 + W4 | 0.688 | 0.615 | 0.125 | 0.400 |
| **v2-slots + W1 + W4** | **0.719** | **0.654** | **0.000** | 0.200 |

**观察**：
- 16 道题的 dev 噪声很大：slots 在 R1 里最差，在 R2 里最好。以后只用 32 道题的 dev 做决定。
- W1（只读玩家发言）和 W4（历史召回）在两轮里都是正向的。
- 仍然存在的失败：小模型把"前天"算错一天；没有事件日期的记忆答不了时间类问题；抽象问法检索不到。

**R3 改动**（对所有变体生效）：
- prompt 里直接给出"前天"的日期；
- 没有 `event_time` 的记忆显示"记于 YYYY-MM-DD"（双时态里的记录时间，相当于"你那天告诉我的"）；
- 写入链路的每次 LLM 调用都限制输出长度（qwen2.5:3b 曾在结构化输出里卡住超过 600 秒）。

**R3（32 题，3 个 seed）**

| 变体 | seed 0 | seed 1 | seed 2 | **均值** | 拒答率 |
|---|---|---|---|---|---|
| **ops + W1 + W4** | 0.750 | 0.656 | 0.656 | **0.687** | 1.000 / 1.000 / 1.000 |
| slots + W1 + W4 | 0.625 | 0.625 | 0.531 | 0.594 | 0.667 / 0.833 / 0.833 |
| ops + W1 + W4 + W5（逐句） | 0.625 | – | – | – | 1.000 |

时间类问题从 R2 的 0.2–0.4 升到 0.6–0.8，来自"前天"日期和"记于"两处改动。

**决定**：采用 ops + W1 + W4，放弃 W2（slots）和 W5（逐句抽取），至少在 3B 模型上如此。单次运行的波动约 ±0.1，所以 test 上基线和候选都取 3 个 seed 的均值。

**预登记**（提交于 test 运行之前）：
- 候选：`PersonalMemory` 默认开启 `player_only=True`、`history_recall=True`（e2e 系统名 `v2+po+history`，检索系统名 `memory-default`），见 `bench/candidate.json`。
- 基线：main @ `72bc605` 的真实代码（在 worktree 中用同一个 runner 跑），e2e 系统名 `v2`。
- 设置：`e2e_v1` test（p_tank、p_marksman2，32 题），qwen2.5:3b，seed 0、1、2，结果合并。
- 门槛：e2e 的 E2EScore 至少比基线高 0.03（improve），拒答率降幅不超过 0.05，过时率升幅不超过 0.05；检索 MemScore@3 降幅不超过 0.02（hold）。

**test 结果（e2e_v1 test，32 题 × 3 seed，按预登记只跑一次）**

| 系统 | E2EScore | Answer@3 | 过时率 | 拒答率 | 写入失败 | 用时 |
|---|---|---|---|---|---|---|
| v0-pipeline | 0.219 | 0.038 | 0.000 | 1.000 | 0 | 3.8 min |
| main（v2） | 0.583 | 0.538 | 0.259 | 0.778 | 1 段对话（失控生成超时） | 26.8 min |
| 候选 v2 + W1 + W4 | 0.594 | 0.577 | **0.148** | 0.667 | 0 | 16.7 min |

候选 − main：ΔE2EScore = +0.010，95% CI [−0.115, +0.135]（先按题目把 3 个 seed 取平均，再做配对 bootstrap），8 题变好、6 题变差。

**门禁：NOT PROMOTABLE**。主指标没达到 +0.03，拒答率下降 0.11，超过 0.05。候选**不进 main**。

**失败分析**（事后看 test，只用于解释，不用于调参）：负例误召回全都是同一种模式，"我喜欢什么颜色 / 运动" → "玩家最喜欢英雄是项羽"。只因为共享了"喜欢"这个谓词就被当成相关，而问的对象（颜色、运动）和记忆里的对象（英雄）根本不同。main 也有同样的误召回，候选只是在更多 seed 里恰好存下了"最喜欢英雄"这种说法。这是**检索层**的问题，不是写入链路本身。

**确实有的收获**：过时率减半（0.259 → 0.148）；写入更快、更稳（没有失控生成，用时 16.7 分钟对 26.8 分钟）；时间类问题从 0.533 升到 0.600。

**下一步**：
- 这次失败之后，e2e_v1 的 test 已经看过，不再用来做晋升决定。下一个候选改用 **e2e_v2 的 test**：它在这次分析**之前**就已封存（`eb8add4`），里面正好有"我喜欢什么电影 / 动物 / 喝什么茶"这类负例，可以公平地检验修复效果。
- 检索层修复方向：谓词类通用词（喜欢、讨厌、想、打算……）只算低权重，要求查询的"对象词"（颜色、运动）至少在字面或语义上命中一次，只在 dev 上开发。

---

## E2 检索：具体词门槛（针对共享谓词误召回）

- **假设**：只靠"喜欢、讨厌"这类通用谓词匹配时不算相关，除非向量相似度 ≥ `min_dense_alone`。
- **retrieval_v2 dev**（新增 8 道此类探针；test 部分有哈希校验，未改动）：MemScore@3 0.872 → 0.949，Recall@3 0.942 → 0.923，Abstain 0.731 → 1.000（`min_dense_alone=0.40`）。
- **e2e dev**（4 个玩家，seed 0；修掉"写入阶段候选检索也继承了门槛"的 bug 之后重跑）：

| e2e dev | 开门槛 | 不开 |
|---|---|---|
| e2e_v1 dev | 0.594 | 0.625 |
| e2e_v2 dev | 0.500 | 0.633 |

拒答率两边都是 1.0；开门槛后更新题和事实题的召回明显下降。
- **结论：放弃作为默认值**，保留为可选项。检索 dev 上的收益来自我针对这个失败模式补的探针；在真实 LLM 写出的记忆上，它挡住的正确召回比错误召回多。
- **不做的事**：W1+W4 候选已经在自己预登记的 test 上失败了。不拿同一个候选换一个 test 集重考（p-hacking）。下一个候选必须带来新的能力（P1）。

**噪声提醒**：同样的配置和 seed，e2e_v1 dev 在 R3 中得 0.750，这次得 0.625。CPU 上的 Ollama 即使固定 seed 也不完全确定，所以关键决定都要看多个 seed。

---

## E3 P1：情景记忆、助手承诺、回忆模式

**第一版（e2e_v2 dev，seed 0）**：E2EScore 0.700（W1+W4 为 0.633）；情景 0.4 → 0.8，承诺 0 → 0.667，时间 → 1.0，但**更新题 1.0 → 0.2**。episode 记着当时的状态，问"我现在什么段位"时把旧值带了回来。

**修正**：
- episode 只回答关于某个时间或事件的问题（"那天、那次、上次、发生了什么"）；
- 没有版本链的变化轨迹，改用各时间点的 facts 和 episodes 按时间重建；
- "那天、发生"等表示时间指向的词加入停用词；
- 承诺不参与普通检索（它会挤掉相关事实），而是像待办清单一样固定放进聊天的 system prompt（`pending_promises`）。

**修正版 dev（2 个 seed 合并；最后一项承诺修改是在这组结果之后做的，这项修改只从普通检索里去掉 promise）**

| | e2e_v2 dev | e2e_v1 dev |
|---|---|---|
| W1+W4（seed 0 / seed 1） | 0.633 / 0.467（均值 0.55） | 0.625 / 0.656（均值 0.64） |
| P1 | **0.783**（情景 1.0，承诺 1.0，时间 1.0，轨迹 0.5，更新 0.6） | 0.609 |

e2e_v1 dev 上的 −0.03 在 seed 间的噪声范围内。写入链路完全相同，更新题的错误主要来自写入：LLM 用 ADD 而不是 UPDATE，导致新旧两个值同时有效，或者漏抽了事实。

**预登记**（提交于 test 运行之前）：
- 候选：`PersonalMemory` 默认开启 `player_only`、`history_recall`、`episodes`、`promises`、`recall_modes`（e2e 系统名 `p1`，检索系统名 `memory-default`），见 `bench/candidate.json`。
- 基线：main @ `72bc605` 的代码（worktree），e2e 系统名 `v2`。
- 设置：**e2e_v2 test**（e_mid、e_support，29 题，封存于 `eb8add4`），qwen2.5:3b，seed 0、1、2，结果合并。
- 门槛：E2EScore 至少比基线高 0.03；拒答率降幅不超过 0.05；过时率升幅不超过 0.05；检索 MemScore@3 降幅不超过 0.02。

**test 结果**（126 个题次 = 42 题 × 3 seed）：

| 系统 | E2EScore | Answer@3 | 事实 | 轨迹 | 拒答 | 过时率 |
|---|---|---|---|---|---|---|
| main P2 | 0.794 | 0.711 | 0.635 | 0.667 | 1.000 | 0.000 |
| **P3** | **0.825** | **0.756** | **0.698** | 0.667 | 1.000 | 0.000 |

门禁**通过**：+0.032 ≥ +0.03，守护指标不变，检索持平（0.912）。

**如实说明**：
- 提升很小，而且集中在两道题上：4 个题次变好、0 个变差。按题聚类的配对 bootstrap 给出 95% CI [0.000, +0.087]，P(Δ ≤ 0) ≈ 0.13，属于边缘显著。按预登记的规则晋升。
- 两道题都是**情景兜底**起的作用："我国庆打算去哪"（成都，3 个 seed 都对）和"我读什么专业"（土木，1 个 seed 对）。答案都只在 episode 里，没被抽成事实。
- 概念标签在 test 上没有带来额外的正确答案。
- 剩下的 19 个事实题失败里，大部分是**写入侧**的问题：小模型没有把事实抽出来，检索无从找起。这是下一步的重点。
- 42 道题太少：像这次 +0.03 左右的效果，现在的 test 集无法稳定分辨。下一个 test 集要更大（目标 ≥ 100 题）。

复现：test 运行按 (seed, 玩家) 分块保存检查点，机器重启只会丢一块；合并后的结果在 `bench/results/e2e_v5_test.json` 和 `bench/results/e2e_v5_test_main.json`，可以用 `--rejudge` 离线重新计分。

**test 结果（e2e_v2 test，29 题 × 3 seed，按预登记只跑一次）**

| 系统 | E2EScore | Answer@3 | 过时率 | 拒答率 | 用时 |
|---|---|---|---|---|---|
| main（v2） | 0.448 | 0.391 | 0.222 | 0.667 | 15.6 min |
| **P1** | **0.759** | **0.783** | **0.111** | 0.667 | 19.4 min |

ΔE2EScore = +0.310，95% CI [+0.138, +0.483]（按题目配对 bootstrap），12 题变好、1 题变差。检索评测持平（retrieval_v2 test MemScore@3 0.892 → 0.892）。**门禁：PROMOTABLE**，合入 main。

分题型（main → P1）：情景 0.067 → 1.000，承诺 0.000 → 0.667，时间 0.500 → 1.000，更新 0.333 → 0.778，事实 0.833 → 0.792，轨迹 0.000 → 0.167，负例 0.667 → 0.667。

**仍然薄弱**：变化轨迹只有 0.167（版本链经常断：小模型用 ADD 而不是 UPDATE，重建出来的故事不完整）；负例拒答率 0.667（和 main 一样，是检索层的老问题）。这两项是下一轮的主要目标。

---

## E4 检索：窄版偏好门槛（`research/personal-memory-consolidation`）

- **问题**：P1 在 e2e_v2 test 上的负例失败全部是同一种模式："我喜欢什么动物 / 喝什么茶" → "玩家最喜欢貂蝉 / 喜欢把字体调大"。
- **为什么 E2 失败**：E2 对**所有**查询都要求命中实词，否则需要余弦 ≥ 0.40。纯换说法、字面上毫无重合的正确匹配（"我为什么老是死" ↔ "被刺客切入"）也被挡住了。
- **E4 的规则**：只有当记忆和查询**仅**共享偏好谓词（喜欢、最喜欢、讨厌、最讨厌、爱）时，才要求余弦 ≥ 0.30；没有任何字面重合的记忆仍按原来的门槛。
- **阈值依据**：负例的余弦在 0.17–0.26；"我最讨厌别人说什么" ↔ "玩家讨厌有人说辅助没用"这个正例是 0.33；其余正例 ≥ 0.54。
- **dev 结果**：
  - retrieval_v2 dev：MemScore@3 0.872 → 0.936，Recall@3 保持 0.942，Abstain 0.731 → 0.923；`min_dense_alone` 在 0.25–0.45 之间结果完全相同。
  - e2e 回放（在已存的 dev 记忆上只重跑读取路径）：e2e_v2 dev 0.767 → 0.783（拒答 0.917 → 1.0）；e2e_v1 dev 0.609 → 0.609。
- **新工具**：`run_e2e --replay` 在已存的记忆上只重跑读取路径，不调用 LLM，读取层的实验几秒钟就能在 e2e 数据上评估。
- **状态**：写进 jina 的默认预设，和巩固（E5）一起作为下一个候选，判定用 e2e_v3 的封存 test。

---

## E5 巩固：段位 / 设备版本链

- **做法**：写入之后，一个确定性步骤识别封闭集合的属性值（段位档位、手机品牌；排除"冲王者"这类愿望句和"王者荣耀"这个游戏名），按时间把旧值依次串进版本链（`consolidate.py`）。LLM 已经建好的部分链会被正确拼接，不会覆盖。
- **c1（e2e_v3 dev，2 个 seed）**：巩固看起来有害（0.672 → 0.603，轨迹 0.5 → 0.167）。查下来发现是它暴露出的两个写入 bug：
  1. "玩家段位是铂金" → "玩家的段位是铂金"这种同义重述被当成新版本，链变成 白银→黄金→铂金→铂金，取前 3 个时丢掉了白银；
  2. 一段完全没提段位的对话里，模型写入了"玩家的段位是钻石"，而该玩家从没到过钻石。
- **修复**：几乎相同的 UPDATE 当作 NOOP；回忆轨迹时合并重复值；新写入的段位或设备值必须在对话原文（UPDATE 时也包括目标记忆）里出现过，否则拒绝写入。
- **c2（修复后，2 个 seed）**：

| | e2e_v3 dev | e2e_v2 dev |
|---|---|---|
| p1（含 E4、E5） | 0.724（轨迹 0.50，更新 0.857） | 0.733（轨迹 0.75，更新 0.70，过时率 0.10） |
| p1 + 巩固 | **0.759**（轨迹 0.667，更新 0.714） | **0.767**（轨迹 1.0，更新 0.80，过时率 0） |

- **预登记**（提交于 test 运行之前）：
  - 候选：`consolidate=True`（默认）+ E4 偏好门槛 + E5 写入修复（e2e 系统名 `p1+consolidate`，检索 `memory-default`）。
  - 基线：main @ `28832ed`（P1）的代码，在 worktree 中运行，e2e 系统名 `p1`。
  - 设置：**e2e_v3 test**（v3_mage、v3_jungle、v3_support，51 题，封存于 `b15eccc`），qwen2.5:3b，seed 0、1、2，结果合并。
  - 门槛：E2EScore 至少比基线高 0.03；拒答率降幅不超过 0.05；过时率升幅不超过 0.05；检索 MemScore@3 降幅不超过 0.02。

**test 结果（e2e_v3 test，51 题 × 3 seed，按预登记只跑一次）**

| 系统 | E2EScore | Answer@3 | 过时率 | 拒答率 | 轨迹 | 更新 | 事实 | 承诺 |
|---|---|---|---|---|---|---|---|---|
| main（P1） | 0.640 | 0.546 | 0.194 | 0.867 | 0.278 | 0.556 | 0.389 | 0.222 |
| 候选（巩固 + E4 + E5） | 0.667 | 0.528 | **0.139** | **1.000** | **0.056** | 0.556 | 0.444 | 0.333 |

Δ = +0.026，95% CI [−0.046, +0.105]，6 题变好、4 题变差。检索 hold 通过（0.892 → 0.912）。**门禁：NOT PROMOTABLE**（需要 +0.03），不合入。

**失败分析**（事后看 test，只用于解释）。巩固本来要改善轨迹，结果轨迹反而从 0.278 跌到 0.056。原因有三：
1. **段位粒度太粗**：识别器只认大段，钻石四 → 钻石一 → 星耀 这条链里，"钻石一"被当成"钻石四"的重复合并掉了。
2. **复合事实被整条作废**：像"玩家是大一新生，在武汉，段位黄金一"这样的复合事实，会因为段位变化被整条标为过时，连带把学校、城市也"忘"了。
3. **手机链经常断**：更早的手机记忆要么没抽到，要么没连进链（有一次模型把"一加13"写成了"加星手机"）。

dev 集没有发现这些问题：dev 玩家的段位变化都跨大段，而且事实比较短。

**确实有效的部分**：拒答 0.867 → 1.000（E4），过时率 0.194 → 0.139。但它们和巩固是打包预登记的，不能事后拆出来单独合入。

**下一步**：
- 段位识别精确到小段；只对"单一属性"的短事实做巩固，复合事实不整条作废；
- 在 dev 上补上小段变化和复合事实的情况；
- e2e_v3 的 test 已经用过，下一个候选需要新的封存 test（e2e_v4）。

---

## E6 按属性回忆（以及 E5 巩固的结论）

**c3（加上小段位和复合事实保护后，2 个 seed）**：巩固在两个 dev 集上**都变差**（e2e_v4 dev 0.722 → 0.667，e2e_v3 dev 0.724 → 0.672）。看了具体记忆之后发现，两个 arm 的 LLM 写入本身差别就很大（例如把"主玩中路"UPDATE 成"主玩中路升级到钻石一"）。**2 个 seed 的 LLM 对比分辨不出 0.03 量级的效果。**

**改进实验方法**：巩固和读取层的改动是确定性的，所以改为在**同一批**已存记忆上回放、做配对比较（`--replay --read-variant consolidated`）。巩固在 6 组 dev 记忆上几乎没有影响（只有一组 +0.017）。**结论：巩固不是关键因素**，默认关闭，代码保留成可选项。同时修掉一个 bug："冲到星耀四了"被当成了愿望句。

**E6 观察**：情景记忆（episode）其实很可靠地记下了每次提到的段位和手机，而事实记忆常被 LLM 合并坏或漏掉。
**E6 做法**：对封闭集合的属性（段位、设备），"怎么变的 / 换过哪些"类问题从所有事实和 episode 中收集这个属性每次被提到的值，按时间排序并合并同一状态的不同说法（"钻石"和"钻石二"算一个），返回最近几个不同的状态；"现在…段位 / 手机"类问题返回最近一次出现的值。

**配对回放（同一批记忆，关闭 → 打开）**：

| dev 记忆 | 关闭 | 打开 |
|---|---|---|
| e2e_v4 dev（c3） | 0.722 | 0.833 |
| e2e_v3 dev（c3） | 0.724 | 0.759 |
| e2e_v3 dev（c2） | 0.724 | 0.759 |
| e2e_v2 dev（c2） | 0.733 | 0.767 |
| e2e_v2 dev（p1b） | 0.767 | 0.817 |
| e2e_v1 dev（p1b） | 0.609 | 0.641 |
| **合并（354 题次）** | | **+0.048，95% CI [+0.028, +0.071]，17 题变好 / 0 题变差** |

在 E6 之上再加巩固，6 组结果完全相同。

**预登记**（提交于 test 运行之前）：
- 候选 P2：P1 + E4 偏好门槛 + E5 写入修复（同义重述视为 NOOP、接地检查、轨迹去重）+ E6 按属性回忆；巩固关闭（e2e 系统名 `p2`，检索 `memory-default`）。
- 基线：main @ `28832ed`（P1）的代码，在 worktree 中运行，e2e 系统名 `p1`。
- 设置：**e2e_v4 test**（v4_jg、v4_sup、v4_top，51 题，封存于 `995e09d`），qwen2.5:3b，seed 0、1、2，结果合并。
- 门槛：E2EScore 至少比基线高 0.03；拒答率降幅不超过 0.05；过时率升幅不超过 0.05；检索 MemScore@3 降幅不超过 0.02。

**test 结果（e2e_v4 test，51 题 × 3 seed，按预登记只跑一次）**

| 系统 | E2EScore | Answer@3 | 过时率 | 拒答率 | 轨迹 | 更新 | 事实 | 情景 | 承诺 | 时间 |
|---|---|---|---|---|---|---|---|---|---|---|
| main（P1） | 0.693 | 0.583 | 0.100 | 0.956 | 0.333 | 0.733 | 0.333 | 0.833 | 0.667 | 0.667 |
| **P2** | **0.765** | **0.694** | **0.000** | 0.933 | **1.000** | 0.767 | 0.292 | 0.833 | 0.667 | 0.667 |

Δ = +0.072（门槛 +0.03），检索 hold 通过（0.892 → 0.912）。**门禁：PROMOTABLE**，合入 main。

如实说明：配对 bootstrap 的 95% CI 为 [−0.007, +0.157]，下限略低于 0，12 道有差异的题里 8 题变好、4 题变差，属于**边缘显著**。收益几乎全部来自轨迹题（0.333 → 1.000）和过时率（0.100 → 0）。事实题（0.333 → 0.292）和拒答率（0.956 → 0.933）有小幅下降，都在容忍度以内，也在 seed 噪声范围内。

---

## E7 事实题：概念标签 + 情景兜底（`research/personal-memory-facts`）

**观察**（dev 记忆）：事实题失败大多是**检索**失败，而不是没存下来。
- "玩家是公交车司机"已经存下，但"我做什么工作"召回不到：问的是类别，记忆里写的是值，字面不重合，小向量模型也分不出它们相近；
- 有些答案只存在于 episode 里，比如"膝盖做了个小手术"，而普通问题不查 episode。

**做法**：
- **概念标签**（`concepts.py`）：用词表给记忆打上它的值所属的类别（职业、城市、位置、健康、专业），给问题打上它问的类别，BM25 匹配共同的标签；
- **情景兜底**：普通问题一条事实都没召回时，再到 episode 里找。

**配对回放**（7 组已存 dev 记忆，406 个题次，基线是 P2 的读取路径）：

| 变体 | Δ | 95% CI | 变好 / 变差 |
|---|---|---|---|
| 概念标签 | +0.034 | [+0.017, +0.052] | 14 / 0 |
| 情景兜底 | +0.044 | [+0.025, +0.067] | 18 / 0 |
| **两者合用** | **+0.081** | **[+0.057, +0.108]** | **33 / 0** |

负例拒答不受影响。概念标签也加到向量文本里没有额外收益，所以只用 BM25 版本。retrieval_v2 dev：0.936 → 0.949。

**已知局限**：e2e_v5 dev 上，两者合用（0.769）低于单用情景兜底（0.808）。"我在哪个城市"通过"城市"标签找到了"老家洛阳"这条事实，于是不再去 episode 里找，而正确答案"郑州"只在 episode 里。

**预登记**（提交于 test 运行之前）：
- 候选 P3：P2 + 概念标签（写进 jina 预设）+ 情景兜底（默认开启）；e2e 系统名 `p3`，检索 `memory-default`。
- 基线：main @ `085c546`（P2）的代码（worktree），e2e 系统名 `p2`。
- 设置：**e2e_v5 test**（v5_t1、v5_t2、v5_t3，42 题，其中事实题 21 道，封存于 `ddee1cd`），qwen2.5:3b，seed 0、1、2。
- 门槛：E2EScore 至少比基线高 0.03；拒答率降幅不超过 0.05；过时率升幅不超过 0.05；检索 MemScore@3 降幅不超过 0.02。
